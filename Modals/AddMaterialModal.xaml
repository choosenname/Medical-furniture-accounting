<Window x:Class="MedicalFurnitureAccounting.Modals.AddMaterialModal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="AddMaterialModal" Height="200" Width="300">
    <StackPanel>
        <Label Content="ADD MATERIAL" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
        <Label Content="Name:" Margin="0 10 0 0"/>
        <TextBox x:Name="MaterialNameTextBox" Margin="10"/>
        <Label Content="Product:"/>
        <ComboBox x:Name="ProductComboBox" Style="{StaticResource ComboBoxStyle}" Margin="10"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Add" HorizontalAlignment="Center" Margin="10" Click="AddButton_Click" />
            <Button Content="Cancel" HorizontalAlignment="Center" Margin="10" Click="CancelButton_Click" />
        </StackPanel>
    </StackPanel>
</Window>