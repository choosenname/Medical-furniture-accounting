<Page x:Class="MedicalFurnitureAccounting.Pages.ProductsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Products Page" Height="450" Width="800">
    <StackPanel Margin="10">
        <StackPanel Orientation="Horizontal" Margin="10" VerticalAlignment="Center">
            <TextBox x:Name="searchBox" Text="Поиск" Style="{StaticResource SearchTextBoxStyle}" GotFocus="SearchBox_GotFocus" LostFocus="SearchBox_LostFocus" />
            <Button Content="Поиск" Click="SearchButton_Click" Style="{StaticResource ButtonStyle}" />
            <Button Content="Сброс" Click="ShowAllButton_Click" Style="{StaticResource ButtonStyle}" />
            <Button Content="Сортировка по name" Click="SortByNameButton_Click" Style="{StaticResource ButtonStyle}" RenderTransformOrigin="0.5,0.5" Width="138" >
                <Button.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform/>
                        <SkewTransform/>
                        <RotateTransform Angle="0.16"/>
                        <TranslateTransform/>
                    </TransformGroup>
                </Button.RenderTransform>
            </Button>

            <Button Content="Сортировка по id" Click="SortByIDButton_Click" Style="{StaticResource ButtonStyle}" Width="122" />

            <ComboBox x:Name="productsFilterComboBox" Style="{StaticResource ComboBoxStyle}" SelectionChanged="ProductsFilterComboBox_SelectionChanged" />

        </StackPanel>

        <StackPanel Height="40" Orientation="Horizontal" HorizontalAlignment="Center">
        <Button Content="Добавить товар" Click="AddProductButton_Click" Style="{StaticResource ButtonStyle}"/>
        <Button Content="Инвенторизация" Click="ShowInventoryList_Click" Style="{StaticResource ButtonStyle}"/>
        </StackPanel>

        <ListView ItemsSource="{Binding Products}" Margin="10" Height="350" Name="productsListView">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Product ID" DisplayMemberBinding="{Binding ProductId}" />
                    <GridViewColumn Header="Category ID" DisplayMemberBinding="{Binding CategoryId}" />
                    <GridViewColumn Header="Material ID" DisplayMemberBinding="{Binding MaterialId}" />
                    <GridViewColumn Header="Supply ID" DisplayMemberBinding="{Binding SupplyId}" />
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="Count" DisplayMemberBinding="{Binding Count}" />
                    <GridViewColumn Header="Room" DisplayMemberBinding="{Binding Room}" />
                    <GridViewColumn Header="Room">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Change Room" Click="ChangeRoomButton_Click" Tag="{Binding ProductId}" Style="{StaticResource ButtonStyle}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Label">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Ярлык" Tag="{Binding ProductId}" Click="Button_Click" Style="{StaticResource ButtonStyle}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                </GridView>
            </ListView.View>
        </ListView>
    </StackPanel>
</Page>